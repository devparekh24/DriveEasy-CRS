import CommonHeader from "../components/CommonHeader/CommonHeader"

const PageNotFound = () => {
    return (
        <CommonHeader heading={'Page Not Found!'} />
    )
}

export default PageNotFound
